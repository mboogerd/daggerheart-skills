# Edge Cases

- If the table wants elements from two frames, recommend one as the base and treat the second as seasoning rather than equal structure.
- If no frame fits, keep the frame structure and propose a custom frame instead of forcing a weak match.
- If players want very broad freedom, present one low-complexity frame plus one custom-frame option.
